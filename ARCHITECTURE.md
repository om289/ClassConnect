# ClassConnect Architecture

A relational school/class portal portal using PHP and MySQL.

## Structure
- `database.php`: MySQL database connection.
- `index.php`: Landing page with role redirection.
- `welcomefaculty.php` / `welcomestudent.php` / `welcomeadmin.php`: Role-based portals.
